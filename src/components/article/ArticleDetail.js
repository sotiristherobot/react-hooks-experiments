import React from "react";

export default function ArticleDetail(props) {
    //TODO Here we need to dispatch an action to redux to get articles if already there
    return (
      <p>Article Detail</p>
    );
}